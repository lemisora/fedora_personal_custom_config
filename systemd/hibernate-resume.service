[Unit]
Description=Disable swap after resuming from hibernation
After=hibernate.target

[Service]
User=root
Type=oneshot
ExecStart=/bin/bash -c "/usr/sbin/swapoff /swap/swapfile" # Here should be the UUID of the swap partition or swapfile

[Install]
WantedBy=hibernate.target
